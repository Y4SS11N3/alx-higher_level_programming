// Changes header color to red using vanilla JavaScript (imported in <head>)
document.addEventListener('DOMContentLoaded', function () {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
});
